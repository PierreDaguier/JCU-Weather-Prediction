services:
  - type: web
    name: jcu-weather-api
    runtime: docker
    plan: free
    healthCheckPath: /health
    autoDeployTrigger: commit
    envVars:
      - key: ALLOWED_ORIGIN
        sync: false
      - key: MODEL_THRESHOLD
        value: "0.5"
